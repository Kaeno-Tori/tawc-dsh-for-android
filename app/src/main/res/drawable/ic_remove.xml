<?xml version="1.0" encoding="utf-8"?>
<!-- The "remove" counterpart to ic_add — the two are the floating menu's
     interface-scale step-down and step-up. Same 24dp viewport and the same
     Material path as the rest of the set, so they sit at one weight. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="@android:color/white"
        android:pathData="M19,13H5v-2h14v2z"/>
</vector>
