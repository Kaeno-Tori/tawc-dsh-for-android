# Gradle
.gradle/
build/
local.properties
**/libs.versions.toml

# Rust
target/

# JNI libs (built artifacts, not source)
**/jniLibs/

# libhybris asset (built by the buildLibhybris Gradle task)
app/src/main/assets/libhybris/

# turnip asset (built by the buildTurnip Gradle task, packed by
# packTurnip — see scripts/build-turnip.sh)
app/src/main/assets/turnip/

# IDE
.idea/
*.iml
.claude/scheduled_tasks.lock
**/*.kate-swp

# OS
.DS_Store

# Local default device selection (see scripts/lib/select-device.sh)
.tawctarget
